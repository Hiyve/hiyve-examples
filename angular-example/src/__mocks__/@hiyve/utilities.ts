/**
 * Mock for @hiyve/utilities
 *
 * This package is not directly imported by any angular-example source files,
 * but may be pulled in transitively. Provides an empty mock to prevent
 * module resolution failures during testing.
 */
export {};
