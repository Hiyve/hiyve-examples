// Empty shim for web-only modules (@mui/material, dayjs) that are imported
// by @hiyve/utilities but not used in the React Native context.
// Metro cannot tree-shake these out, so we provide an empty module instead.
module.exports = {};
