@hiyve:registry=https://console.hiyve.dev/api/registry/

# @hiyve/utilities has web-only peer deps (@mui/material, @emotion/*)
# that are not needed in React Native. Skip peer dep resolution to
# avoid pulling in react-dom and MUI into the RN dependency tree.
legacy-peer-deps=true
