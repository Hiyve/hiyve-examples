@hiyve:registry=https://api.muziemedia.com/registry/
shamefully-hoist=true

# @hiyve/rn-react peer dep specifies react ^18.0.0 but RN 0.83 uses React 19.
# Safe to ignore — React 19 is fully compatible. Remove once SDK peer dep is updated.
legacy-peer-deps=true
