# Hiyve SDK Credentials — get these from console.hiyve.dev

# API Key (required — public key, safe for client-side use)
VITE_HIYVE_API_KEY=pk_test_xxx

# Environment (development or production)
VITE_HIYVE_ENVIRONMENT=development
