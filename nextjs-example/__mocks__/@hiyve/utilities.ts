/**
 * Mock for @hiyve/utilities
 *
 * Provides mock implementations of utility functions used
 * by the nextjs-example components.
 */

export const formatHiyveError = jest.fn((msg: string) => `Formatted: ${msg}`);
